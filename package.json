{
  "name": "codex-grok-task-bridge",
  "version": "0.4.0",
  "type": "module",
  "bin": {
    "grok-task-bridge-stdio": "bin/bridge-mcp-stdio.mjs"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "test:oauth:local": "node scripts/oauth-smoke.mjs",
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy:staging": "wrangler deploy --env staging",
    "smoke": "node scripts/e2e-smoke.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260917.1",
    "tsx": "^4.19.2",
    "typescript": "^5.9.2",
    "wrangler": "^4.131.1"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.3"
  }
}
